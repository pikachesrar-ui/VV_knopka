# VV_knopka — LIVE PROGRESS

Обновлено 2026-09-24.

## 2026-09-20 — продолжение серии после неудачного сюжета

- После окончательной блокировки AI-слота текущий цикл немедленно переходит к
  следующему слоту, не расходуя отдельную попытку ручной серии; максимум один
  такой переход на один цикл. Slot 33 остаётся BLOCKED, 34 — следующий.
- Поиск и скачивание стоковых роликов восстанавливаются после коротких сетевых
  обрывов: максимум три GET, паузы 2 и 6 секунд. Долгий сетевой сбой
  по-прежнему завершает серию после существующего лимита трёх циклов.
- Оплата дополнительных OpenAI-вызовов при восстановлении AI-слота ограничена
  существующим резервом и ledger; новые сетевые повторы OpenAI не вызывают.
- 216 локальных тестов PASS; Windows-верификация и реальная публикация 34
  остаются за пользователем после `git pull`.

## 2026-09-20 — установлена причина остановки ручной серии

- Первый цикл: slot 33 «octopus» прошёл поиск двух одобренных источников из
  требуемых трёх; запасной сценарий не прошёл фактчекинг. Слот заблокирован.
- Следующие попытки дошли до `render-animal 34`, но внешнее соединение
  оборвалось (`httpx.ConnectError`, WinError 10054). Batch завершился
  `failed`, `completed=0`; `pending ready uploads=0`.
- Ошибки DNS YouTube Analytics не блокировали публикацию; 403 OpenAI в 03:32
  относится к отдельному ночному запуску.
- Журнал runner больше не буферизует вывод до завершения многоминутной команды:
  дочерний stdout/stderr пишется сразу; Python выводит строки без буферизации.
  Этот этап не тратит OpenAI API и не меняет защитные ограничения.
- На Windows остаются проверка сети и штатный повтор после `git pull`;
  успешная публикация 33/34 ещё не подтверждена. Локальный расход $0.6413/$10.

## 2026-09-20 — экспорт аналитики и календарь выпуска

- Bundle `vv-analytics-20260920-173107.zip`: 32 ролика; 1652 снимка;
  66 checkpoint rows; локальное время: 17.09 три публикации (27–29),
  18.09 одна (30), 19.09 одна (31), 20.09 одна (32 в 01:33).
- Slots 27–32 на 17:31 UTC: 933, 1167, 1028, 1409, 1070, 1661 views.
  Slot 24 4037 views; рост охвата относительно ранних 1–22 несомненный,
  причину роста без корректного сравнения удержания не приписывать алгоритму.
- Analytics API ещё не отдаёт 31/32; точный Stayed to watch в ZIP отсутствует.
- Ручная серия сегодня: requested=3, completed=0, failed после трёх попыток
  с 04:18 до 13:43 МСК. Журнал получен позднее; причина указана выше.
  Ранее известный 403 в 03:32 сам по себе не доказывает причину этих попыток.

## 2026-09-20 — сверка фактической публикации

- Slots 31/32 подтверждены `VERIFIED_PUBLIC`. На 03:30 у slot 32:
  583 views / 28 likes. `stats` в 17:31 UTC: 32 receipt videos, slot 33 пока
  не подтверждён.
- Deep Analytics вернул 2/4 rows по 29-35, для 31/32 данные пока не готовы;
  bundle впоследствии передан и разобран; точный Stayed to watch отсутствует.
- Ранее в 03:32 OpenAI planning вернул 403, Analytics DNS ошибся отдельно;
  причины ручной серии подтверждены позднее журналом runner (см. выше).
- Интерактивный `stats` больше не падает на CP1251 при эмодзи в названии:
  заменяем только неподдерживаемые символы на `\\U...` для печати.
- 213 локальных тестов PASS; платных запросов API при исправлении не было.

## 2026-09-19 — первая стадия устойчивого конвейера

- AI-слот после проверенного отказа фактчека или исчерпанных видеоисточников
  один раз меняет тему, сохраняя архив и локальный статус восстановления.
- Второй подтверждённый отказ блокирует этот слот и освобождает очередь для
  следующего; HTTP и неизвестные ошибки не приводят к пропуску слота.
- Резерв по бюджету перед заменой: оценочно $0.51, допустимый максимум $0.55;
  общий лимит OpenAI $10 действует. У нормального выпуска нет новых API-вызовов,
  однако изменённый prompt котов может добавить немного входных токенов.
- Review котов больше не подталкивается исходным придуманным title/hook;
  заголовок собирается по подписи к фактически выбранному первому кадру.
- 212 локальных тестов PASS; OpenAI API в ходе
  разработки не вызывался. Windows-публикация, идущая сейчас у пользователя,
  работает на ранее установленном коде, её результат отдельно не проверен.
- Следующий этап: на новых Shorts измерить точность caption/кадра, затем
  экономный Hook Engine и выбор долей категорий по достаточной выборке.

## Подтверждено файлами пользователя 2026-09-05

20 опубликованных видео со статусом public; pending=0; 66 просмотров проекта.
AI: 39 на 10 видео; cats: 27 на 10 видео. Комментариев нет.
Slots 17–20 созданы/загружены. Следующий ожидаемый на момент снимка — 21 AI EN.
Текущий расход API не получен; бюджет $10 сохраняется.

## Реализовано в коде

- direct_v1 для новых renders со slot 21;
- котики: 3–4 сильных клипа из проверенного pool, без карточек;
- конкретные подписи и title/description из существующего highlight review;
- сильный первый/последний момент; quality gate вместо заполнения слабым материалом;
- AI: exact hook opens script, ранний ответ, opening metadata heuristic,
  sequential curated materials / 4 seconds;
- защита существующих MP4 от ручного overwrite через plan/render CLI;
- editorial_profile в upload sidecar и stats;
- output cap planner=1800 tokens, учёт расхода до разбора/валидации ответа;
- новых платных стадий нет.

178 tests PASS; FFmpeg smoke PASS (video/audio/captions/no cards).
Реальный MPT/Windows новый выпуск ещё не проверен. Это не подтверждение роста просмотров.

Команды применения, ограничения и анализ: [EDITORIAL_RU.md](EDITORIAL_RU.md).
Следом — локальный git pull, тесты и обычный scheduler цикл; сырые ручные
`longrun-next`/`upload-ready` не использовать, для серии есть поддерживаемый ярлык.
Пилот 1–15 и ранее готовые видео сохраняются. TikTok вне scope. PR #1 не merge.

## Получены slot 21/22 и выровнен звук будущих cats

- 21 готов 2026-09-06 00:35 UTC, 22 — 02:54 UTC, оба до коммита direct_v1
  (09:56 UTC), поэтому по ним новый монтаж ещё не оценить.
- У 22 ~5.7 секунд чёрных карточек; пики кошачьих клипов ~-1 dBTP,
  мяуканья ~-8 dBTP; средний звук большинства фрагментов близок к -16 LUFS.
- Для будущих cats: умеренная компрессия перед loudnorm, пик -8 dBTP,
  дополнительный limiter. Исходный тихий clip не усиливаем до громкого шума.
- Реальный FFmpeg-тест на звуке slot 22: пик тихого фрагмента снизился
  с ~-1 до ~-7.6 dBTP; 179 тестов PASS; новые опубликованные MP4 не трогали.
- Windows Python 3.11 не прочитал тестовый WAVE_FORMAT_EXTENSIBLE через stdlib
  `wave`; production filter успешно выполнился. Тест длительности переведён на
  ffprobe для Windows/Linux. Расход пользователя: $0.3087 / $10, gate PASS.


## 2026-09-14 — analytics storage v0

- Slot 23 повторно спланирован вручную после временного OpenAI 403; fact-check PASS,
  `runtime/slots/23/plan.json` создан. Ключ и модели Terra/Luna доступны.
- Добавлено теневое SQLite-хранилище всех снимков `vv-youtube stats`.
- Контрольные точки 24/72/168 часов выбирают первый доступный снимок после цели.
- Ошибка SQLite не блокирует публикацию; JSON/JSONL остаются резервным журналом.
- Новый этап не вызывает OpenAI и стоит $0 API.


- Добавлен идемпотентный `analytics-import-history`: переносит накопленный
  `statistics-history.jsonl` в SQLite, пропускает повреждённые строки и не
  дублирует уже импортированные снимки.


## 2026-09-14 — Auto-QA v0

- Локальные FFmpeg/ffprobe-проверки добавлены перед scheduler upload в shadow mode.
- QA пишет `*.qa.json`, но пока не блокирует уже работающую публикацию.
- Проверяются stream/resolution/duration, dark borders/opening/tail, loudness/peak,
  конфигурация voice/music, safe-zone снизу, SRT, outro CTA, pacing и source markers.
- Правая safe zone, pixel watermark и точный voice/music ratio честно отмечаются
  как SKIP, пока нет надёжного локального CV/OCR/stem анализа.
- OpenAI API не используется; стоимость этапа $0.

## 2026-09-15 — rich analytics v1

- Добавлен безопасный OAuth upgrade `auth-analytics` с проверкой старого channel binding.
- Core sync сохраняет engaged views, watch time, AVD/APV, реакции и подписки.
- Scheduler запускает его best-effort максимум раз в 20 часов; публикация не блокируется.
- Ручной `--deep` получает источники трафика и точки audience retention.
- Русские/английские Studio CSV/ZIP импортируются локально и идемпотентно.
- `analytics-export` собирает один secret-free ZIP для передачи на будущий анализ.
- SQLite schema v2 обновляла базу без удаления 465 уже импортированных snapshots.
- 193 tests PASS; OpenAI API и платные провайдеры не вызываются ($0).

## 2026-09-15 — проверка первого analytics bundle

- Bundle валиден: 23 bot-видео, 557 snapshots; secrets отсутствуют.
- Slot 23: 174 Studio views к 13.6h и 9 likes; прежний максимум — 22 views.
- YouTube Analytics API вернул 0 строк для свежего ролика, поэтому AVD/APV,
  traffic и retention пока честно остаются отсутствующими.
- Удалена запись ложного zero snapshot при API lag; старые такие строки
  очищаются автоматически.
- Checkpoints принимают снимок только в пределах 24h после цели; слишком поздние
  старые значения очищаются.
- Schema v3 хранит duration из Studio; cat-fact slot 23 получает category `cats`
  после повторного импорта/снимка. 198 tests PASS, OpenAI cost $0.

## 2026-09-16 — ручная серия и slot 24

- Trigger 01:30 дошёл до slot 24, но Pexels оборвал чтение по timeout; pending=0.
- Ручной повтор нормального runner восстановился без специальных обходов.
- Slot 24 опубликован public в 03:45: `https://www.youtube.com/watch?v=TujKlWJL-Eo`.
- QA: WARN, critical=0, warnings=6. Перед циклом slots 1–23 VERIFIED_PUBLIC.
- Ledger после планирования/рендера: $0.3314 / $10.
- Добавлен `start-three-video-batch.ps1`: три последовательных безопасных цикла,
  целевой интервал публикаций 60 минут, общий runner lock и fail-closed остановка.
- Успешная ручная серия подавляет ночные 01:30/03:30/05:30 до 06:30; при ошибке
  ночное восстановление снова разрешено.
- `install-manual-batch-shortcut.ps1` создаёт Desktop/Start Menu ярлыки для
  последующего закрепления кнопки на панели задач.


## 2026-09-17 — одна кнопка доводит серию до трёх публикаций

- Ручная серия в 13:26 увидела slots 1–26 VERIFIED_PUBLIC и pending=0.
- Slot 24: 1520 views / 35 likes; slot 26: 322 views / 16 likes.
- Первый новый slot 27 был отклонён fact-check, поэтому старая версия batch
  остановилась с completed=0 и вернула управление ночному scheduler.
- Теперь неудачная попытка не считается публикацией и автоматически повторяется:
  максимум 3 попытки на каждый из трёх успешных выпусков, пауза 30 секунд.
- Все fact-check/source/QA/upload gates и защита от дублей сохранены.
- После исчерпания трёх попыток пакет всё ещё останавливается безопасно, чтобы
  не зациклиться и не расходовать OpenAI API бесконтрольно.
- Последний показанный расход: $0.3848 / $10.

## 2026-09-19 — восстановление после нехватки кадров для slot 31

- Пользователь подтвердил: slots 1–30 VERIFIED_PUBLIC, pending=0; slot 30 вышел
  18 сентября, а slot 31 про осьминога остановился на 2/3 одобренных видео.
- Ручная серия трижды повторяла неисправимый для этой темы запрос и завершалась
  до публикации первого ролика. Данные slot 31 пользователь сохранил в
  `runtime/recovery`.
- Попытка сменить тему на кошек и коробки была отклонена fact-check из-за
  неподтверждённого утверждения о железах на лапах; готового нового плана нет.
- Исправлен сброс статистики Pexels/Pixabay старого `ai_materials.json` при
  изменении visual_anchor. Новый план заново ищет релевантные кадры; условия
  отбора и фактчекинг остаются прежними. 202 теста прошли локально, API $0.
- Нужен `git pull` и успешная замена плана slot 31 на ПК пользователя, затем
  штатный запуск ярлыка. Публикация slot 31 пока не подтверждена.

## 2026-09-22 — terminal recovery для cats и безопасные stock retries

- Реальный manual batch корректно пропустил ранее заблокированный slot 33, но
  трижды повторил slot 36: полный поиск дал только 2/5 подходящих cat-клипов;
  одна попытка получила временный Pixabay HTTP 503.
- Завершённый свежий аудит обоих настроенных стоков теперь может пометить только
  этот animal slot как `BLOCKED`; текущий запуск продолжает следующий слот в
  рамках общего лимита одного нового blocked-skip.
- Старый аудит, незавершённый поиск, отсутствие ключа одного провайдера и
  сетевой сбой не считаются доказательством исчерпания источников.
- Stock GET повторяет HTTP 429/500/502/503/504 и transport errors максимум три
  раза. Ошибки больше не включают query-параметры/API-ключи.
- Порог 5 релевантных вертикальных клипов со слышимым звуком не снижен.
- 220 tests PASS; дополнительный OpenAI cost этой логики — $0.


## 2026-09-24 — batch больше не зависит от окна ярлыка

- Рабочий batch запускается отдельным скрытым PowerShell-процессом; stdout/stderr
  перенаправлены в файлы, а открытое окно только читает журналы.
- Выделение, копирование или закрытие окна наблюдения больше не может остановить
  Python/FFmpeg. Повторный клик подключается к уже работающему worker после
  проверки PID/command line.
- Сохранены locks, receipts, fail-closed gates, три попытки, интервал 60 минут и
  подавление ночных триггеров. Стоимость изменения: $0 API.
- Пять статических regression-тестов прошли локально. После pull нужно
  переустановить ярлык и выполнить полный Windows pytest.
